server {

      listen <%= port %>;
      server_name <%= host %>;
      root <%= root %>;

      access_log <%= scope.lookupvar('nginxpassenger::logdir') %>/<%= host %>_access.log;
      error_log  <%= scope.lookupvar('nginxpassenger::logdir') %>/<%= host %>_error.log;

}
